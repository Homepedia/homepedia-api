version: '3.9'

services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: homepedia
      POSTGRES_PASSWORD: epitech
      POSTGRES_DB: auth
    ports:
      - "5434:5432"
